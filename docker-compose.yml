services:
  web:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

  test:
    build:
      context: .
      dockerfile: app/Dockerfile
    command: pytest
    depends_on:
      - web
